// Prisma loads every .prisma file in this directory as one schema.
// Keep datasource and generator configuration here; put models in domain files.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
